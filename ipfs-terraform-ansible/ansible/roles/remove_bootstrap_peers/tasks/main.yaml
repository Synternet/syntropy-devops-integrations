---
- name: Remove all peers in Bootstrap list
  command: "docker exec -it {{inventory_hostname}} ipfs bootstrap rm --all"
# - name: Remove swarm peers
#   command: "docker exec -it {{inventory_hostname}} ipfs swarm disconnect $(ifps swarm peers)"
